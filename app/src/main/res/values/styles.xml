<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary_pink</item>
        <item name="colorPrimaryDark">@color/primary_pink_dark</item>
    </style>

    <!-- style for the ChatActivity, needed to add actionbar with back button and User Name as title -->
    <style name="ChatTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary_pink</item>
        <item name="colorPrimaryDark">@color/primary_pink_dark</item>
    </style>

</resources>
